import {BiontView} from 'biont-backbone';

export default class Grid extends BiontView.extend({}) {
    constructor(data, args) {
        super(data, args);
        this.rendered = false;
    }

    render(forced) {

    }

}